============================= test session starts =============================
platform win32 -- Python 3.14.3, pytest-9.0.2, pluggy-1.6.0
rootdir: C:\Users\Ankit.Sadhu1\Desktop\python\chess_game
plugins: anyio-4.12.1, langsmith-0.7.6, asyncio-1.3.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_fixture_loop_scope=None, asyncio_default_test_loop_scope=function
collected 56 items

tests\test_board.py ..............                                       [ 25%]
tests\test_check_detection.py .......F

================================== FAILURES ===================================
_____________________________ test_back_rank_mate _____________________________

    def test_back_rank_mate():
        """Classic back-rank mate: rook delivers on 8th rank."""
        b = Board()
        # White Ka1, Rd1; Black Ke8 boxed in by own pawns d7,e7,f7,g7,h7
        b.set_piece(Position(0, 0), Piece(Color.WHITE, PieceType.KING, has_moved=True))
        b.set_piece(Position(0, 3), Piece(Color.WHITE, PieceType.ROOK, has_moved=True))
        b.set_piece(Position(7, 4), Piece(Color.BLACK, PieceType.KING, has_moved=True))
        b.set_piece(Position(6, 3), Piece(Color.BLACK, PieceType.PAWN, has_moved=True))  # d7
        b.set_piece(Position(6, 4), Piece(Color.BLACK, PieceType.PAWN, has_moved=True))  # e7
        b.set_piece(Position(6, 5), Piece(Color.BLACK, PieceType.PAWN, has_moved=True))  # f7
        b.set_piece(Position(6, 6), Piece(Color.BLACK, PieceType.PAWN, has_moved=True))  # g7
        b.set_piece(Position(6, 7), Piece(Color.BLACK, PieceType.PAWN, has_moved=True))  # h7
    
        game = GameController(b)
>       game.make_move_algebraic("d1", "d8")  # Rd8#
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests\test_check_detection.py:98: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <chess_engine.game.GameController object at 0x000002DE71FABD90>
from_str = 'd1', to_str = 'd8', promotion = None

    def make_move_algebraic(self, from_str: str, to_str: str,
                            promotion: Optional[PieceType] = None) -> Move:
        """Convenience: make_move using algebraic strings like 'e2', 'e4'."""
>       return self.make_move(
            Position.from_algebraic(from_str),
            Position.from_algebraic(to_str),
            promotion,
        )

chess_engine\game.py:178: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <chess_engine.game.GameController object at 0x000002DE71FABD90>
from_pos = Position(d1), to_pos = Position(d8), promotion = None

    def make_move(
        self,
        from_pos  : Position,
        to_pos    : Position,
        promotion : Optional[PieceType] = None,
    ) -> Move:
        """
        Execute a move.  Raises ValueError if illegal.
        Returns the executed Move with check/checkmate annotations.
        """
        if self.is_game_over:
            raise ValueError(f"Game is already over ({self._status.value})")
    
        piece = self.board.get_piece(from_pos)
        if piece is None:
            raise ValueError(f"No piece at {from_pos}")
        if piece.color != self.current_turn:
            raise ValueError(f"Not {piece.color.value}'s turn")
    
        # Find matching legal move
        legal = self.get_legal_moves()
        candidates = [
            m for m in legal
            if m.from_pos == from_pos and m.to_pos == to_pos
        ]
    
        if promotion:
            candidates = [m for m in candidates if m.promotion == promotion]
        elif any(m.promotion for m in candidates):
            # Default to queen promotion
            candidates = [m for m in candidates if m.promotion == PieceType.QUEEN]
    
        if not candidates:
>           raise ValueError(
                f"Illegal move: {from_pos} \u2192 {to_pos} for {piece.piece_type.value}"
            )
E           ValueError: Illegal move: d1 \u2192 d8 for ROOK

chess_engine\game.py:124: ValueError
=========================== short test summary info ===========================
FAILED tests/test_check_detection.py::test_back_rank_mate - ValueError: Illeg...
!!!!!!!!!!!!!!!!!!!!!!!!!! stopping after 1 failures !!!!!!!!!!!!!!!!!!!!!!!!!!
======================== 1 failed, 21 passed in 0.27s =========================
